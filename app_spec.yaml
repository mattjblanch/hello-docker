name: hello-docker
services:
  - name: web
    image:
      registry_type: DOCKER_HUB
      registry: registry.hub.docker.com
      repository: mattjblanch/hello-docker
      tag: latest
    internal_ports:
      - 3000